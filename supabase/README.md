# Supabase Connection 
- need to have SUPABASE_ACCESS_TOKEN or ```supabase login``` to run supabase over cli 
- migrations folder contains commands from past database

```supabase migration list```: shows all migration files tracked  
```supabase db push```: pushes all migration files/changes onto the remote (actual database)