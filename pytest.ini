[pytest]
# Repo root on sys.path so `import backend.*` resolves the same way it does
# under `python -m uvicorn backend.app:app`.
pythonpath = .
testpaths = backend/tests
markers =
    integration: exercises a real local Supabase/Postgres; requires `supabase start`
